const floorsInput = document.querySelector("#number-of-floors");
const liftsInput = document.querySelector("#number-of-lifts");
const creationForm = document.querySelector(".form");
const submitBtn = document.querySelector(".submitBtn");
const liftSection = document.querySelector(".lift-simulation");

creationForm.addEventListener("submit", (e) => {
  e.preventDefault();
  /**
   *
   * Generate lift layout
   */
});

/**
 *
 * @param {Number} floors Number of floors
 * @param {Number} lifts Number of lifts
 */
function createLiftAndFloors(floors, lifts) {}
